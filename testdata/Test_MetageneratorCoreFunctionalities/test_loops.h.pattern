=========================

before${#for f in listN} sommething:${f}${#end} after

before
${#for f in listN}
sommething:${f}
${#end}
after

before${#for f in listN}
sommething:${f}${#end}
after

before
${#for f in listN}sommething:${f} ${#end}
after

before
${#for f in listN}sommething:${f}
${#end}after

=========================

before${#for f in list0} sommething${#end} after

before
${#for f in list0}
sommething
${#end}
after

before${#for f in list0}
sommething${#end}
after

before
${#for f in list0}sommething${#end}
after

before
${#for f in list0}sommething
${#end}after

=========================
