${#include "header.pattern"}

#include <sstream>

#include "${header_file_name.get_relative_to(source_file_name.parent())}"
${#for include from includes}#include "${include.get_relative_to(source_file_name.parent())}"
${#end}

// -vvv YOU CAN PUT ADDITIONAL INCLUDES BELOW

// -^^^ END OF USER DEFINED INCLUDES

${#include "namespace.wrapper.pattern" "code_body_pattern": "enum.cpp.body.pattern"}
