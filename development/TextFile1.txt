
---------------------------------------------------------------

${#include "header.pattern"}

#pragma once

#include <cstdint>
#include <string>
#include <iostream>

#include "binary_serialization.h"

${#include "namespace.wrapper.pattern" "code_body_pattern": "bitflags.h.body.pattern"}

---------------------------------------------------------------

{"code_body_pattern": "bitflags.h.body.pattern"}

---------------------------------------------------------------

${#if namespaces != []}
namespace ${namespaces[0]}
{
  ${#include "namespace.wrapper.pattern" "namespaces": namespaces[1:]}
} // end ${namespaces[0]}
${#else}
${#include code_body_pattern}
${#end}

---------------------------------------------------------------

